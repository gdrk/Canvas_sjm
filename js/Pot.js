var Pot = Class.extend({
   init : function(){

   },
    changetype : function(){

    },
    render : function() {

    }
});


if(event.offsetY < 180 || event.offsetY > 468 || event.offsetX < 20 || event.offsetX > 335) return;
